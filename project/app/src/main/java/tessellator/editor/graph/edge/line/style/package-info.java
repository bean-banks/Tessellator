package tessellator.editor.graph.edge.line.style;

/**
 * The 'style' package groups together the classes that represent the styles
 * that the line component of an edge can be styled with. The package also holds an
 * enum which records all the available styles. When a style class is added to this
 * package, make sure to add an option to the LineStyle enum that represents the class.
 */