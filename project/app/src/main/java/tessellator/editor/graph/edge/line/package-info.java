package tessellator.editor.graph.edge.line;

/**
 * The 'line' package groups together the classes relevant for creating a
 * customised line component of an edge. The line itself is created with
 * the shape factories under the 'shape' package, whiile the line's
 * appearance is styled by style classes within the 'style' package. The
 * LineFactory classes applies both of these processes on one location
 * to produce a full custom line.
 */