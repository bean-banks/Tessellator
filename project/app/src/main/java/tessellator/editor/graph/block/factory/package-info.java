package tessellator.editor.graph.block.factory;

/**
 * This package groups together BlockFactory class and all of
 * its subclasses. Any one wishing to create a block within the
 * graph builder should always use these factories over directly
 * using the block categories.
 */